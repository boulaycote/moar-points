Point = function (document) {
  this.x = document.x;
  this.y = document.y;
  this.r = document.r;
}

// Seulement sur le client
if (Meteor.isClient) {

}

// Seulement sur le serveur
if (Meteor.isServer) {

}
